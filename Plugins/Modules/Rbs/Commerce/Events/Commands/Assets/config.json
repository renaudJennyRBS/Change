{
	"rbs_catalog:export-attributes": {
		"description": "Export Attributes",
		"options": {
		},
		"arguments": {
			"fileName": {
				"description": "json file path",
				"required": true,
				"default": null
			}
		}
	},
	"rbs_catalog:import-attributes": {
		"description": "Import Attributes",
		"options": {
		},
		"arguments": {
			"fileName": {
				"description": "json file path",
				"required": true,
				"default": null
			}
		}
	},
	"rbs_commerce:initialize-web-store": {
		"description": "Initialize a store structure",
		"options": {
			"userAccountTopicId": {
				"description": "use existing user account topic instead of creating a new one",
				"default":""
			},
			"override": {
				"description": "override existing documents",
				"default":"false"
			}
		},
		"arguments": {
			"websiteId": {
				"required": true,
				"description": "the website in which you want to initialize structure"
			},
			"storeId": {
				"required": true,
				"description": "the store for which you want to initialize structure"
			},
			"LCID": {
				"required": true,
				"description": "the LCID for labels (ex: en_US)"
			},
			"sidebarTemplateId": {
				"required": true,
				"description": "the template for page using sidebar style"
			},
			"noSidebarTemplateId": {
				"required": true,
				"description": "the template for page using no sidebar style"
			}
		}
	},
	"rbs_commerce:initialize-order-process": {
		"description": "Initialize a order process structure",
		"options": {
			"userAccountTopicId": {
				"description": "use existing user account topic instead of creating a new one",
				"default":""
			},
			"override": {
				"description": "override existing documents",
				"default":"false"
			}
		},
		"arguments": {
			"websiteId": {
				"required": true,
				"description": "the website in which you want to initialize structure"
			},
			"storeId": {
				"required": true,
				"description": "the store for which you want to initialize structure"
			},
			"LCID": {
				"required": true,
				"description": "the LCID for labels (ex: en_US)"
			},
			"sidebarTemplateId": {
				"required": true,
				"description": "the template for page using sidebar style"
			},
			"noSidebarTemplateId": {
				"required": true,
				"description": "the template for page using no sidebar style"
			},
			"popinTemplateId": {
				"required": true,
				"description": "the template for page used in popin"
			}
		}
	},
	"rbs_commerce:initialize-return-process": {
		"description": "Initialize a return process structure",
		"options": {
			"userAccountTopicId": {
				"description": "use existing user account topic instead of creating a new one",
				"default":""
			},
			"override": {
				"description": "override existing documents",
				"default":"false"
			}
		},
		"arguments": {
			"websiteId": {
				"required": true,
				"description": "the website in which you want to initialize structure"
			},
			"storeId": {
				"required": true,
				"description": "the store for which you want to initialize structure"
			},
			"LCID": {
				"required": true,
				"description": "the LCID for labels (ex: en_US)"
			},
			"sidebarTemplateId": {
				"required": true,
				"description": "the template for page using sidebar style"
			},
			"noSidebarTemplateId": {
				"required": true,
				"description": "the template for page using no sidebar style"
			},
			"printTemplateId": {
				"required": true,
				"description": "the template for page used for print"
			}
		}
	},
	"rbs_stock:check-inventory-entries": {
		"description": "Check inventory entries for all sku and warehouse",
		"options": {
			"refreshAvailability": {
				"description": "force refresh availability of products",
				"shortcut": "r"
			}
		},
		"arguments": {
		}
	}
}