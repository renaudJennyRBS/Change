{% import 'block-parameters.twig' as render %}

<div>
	{% for param in templateInformation.parametersInformation %}
		{% if not param.hidden %}
			{{ render.param(param, information.name ~ "_" ~ param.name) }}
		{% endif %}
	{% endfor %}
</div>