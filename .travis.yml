language: php

php:
  - 5.3
  - 5.4
  - 5.5

before_install:
 - curl -s https://getcomposer.org/installer | php -- --quiet
 - php composer.phar install --dev --no-interaction 
 - mysql -e 'create database rbschangetest;'
 - bash travisconfig/init.sh

script:
 - cd ChangeTests/
 - phpunit Change/

notifications:
   email:
       - noreply@rbschange.fr
